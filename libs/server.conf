[global]
request.show_tracebacks = False
server.thread_pool = 10
server.show_tracebacks = False

[/]
tools.staticdir.on = True
tools.staticdir.dir = '/data/libs'

# As we are taking files from /data/libs, unset the root
tools.staticdir.root = None

# Do not expose hidden files in the root (e.g. .git)
tools.staticdir.match = '^/[a-zA-Z0-9]'

